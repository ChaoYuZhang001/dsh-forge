{
  "name": "dsh-gate",
  "version": "0.4.0-alpha.3",
  "description": "Static compatibility and permission verification for DeepSeek Harness plugins.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "dsh-gate": "dist/cli/main.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist",
    "schemas",
    "README.md",
    "LICENSE",
    "SECURITY.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/verifier.test.mjs tests/matrix.test.mjs tests/catalog-contract.test.mjs tests/provider-smoke.test.mjs",
    "check": "npm run build && npm test",
    "verify:fixture": "npm run build && node dist/cli/main.js verify fixtures/public/healthy-plugin --smoke",
    "verify:provider": "node scripts/provider-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.7.2"
  }
}
